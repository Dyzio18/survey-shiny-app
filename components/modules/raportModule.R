# Raport Module
# Generate raport and export to PDF

raportModuleUI <- tabItem(
    tabName = "raport",
    h2("Raport"),
    box(width = 12, class="blankPage",
        div(
          h3('No items', class="blankPage__message")
        )
    )
)

