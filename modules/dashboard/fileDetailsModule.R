fileDetailsModuleUI <- function(id) {
    ns <- NS(id)
    column(12, 
        h2("MODUL")
    )
}

# Functionality for dataselection for plot
# SelectInput is rendered dynamically based on data

fileDetailsModule <- function(input, output, session, data) {

}